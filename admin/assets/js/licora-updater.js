(function(){
'use strict';
var initial=window.LicoraUpdaterInitial||{};
var state={update:initial.update||{},job:initial.active_job||null,preflightVersion:null,lastEventId:0,driving:false,totalEvents:0,retryMs:700};
var csrf=(document.querySelector('meta[name="csrf-token"]')||{}).content||'';
function el(id){return document.getElementById(id);}function sleep(ms){return new Promise(function(r){setTimeout(r,ms);});}
async function api(url,options){options=options||{};var headers=Object.assign({'Accept':'application/json'},options.headers||{});if(options.method==='POST'){headers['Content-Type']='application/json';headers['X-CSRF-Token']=csrf;}var response=await fetch(url,{method:options.method||'GET',headers:headers,body:options.body?JSON.stringify(options.body):undefined,credentials:'same-origin',cache:'no-store'});var data;try{data=await response.json();}catch(e){throw new Error('Updater returned a non-JSON response (HTTP '+response.status+').');}if(!response.ok||data.success===false){var err=new Error(data.message||('Updater request failed (HTTP '+response.status+').'));err.code=data.code||'UPDATE_REQUEST_FAILED';throw err;}return data;}
function showPageError(message){var box=el('updater-page-error');if(!box)return;box.textContent=message||'';box.hidden=!message;}
function fmtTime(epoch){if(!epoch)return'Not yet';try{return new Date(epoch*1000).toLocaleString();}catch(e){return String(epoch);}}
function updateReleaseUI(data){state.update=data||{};var latest=data.latest||null;el('current-version').textContent=data.current_version||'';el('last-checked').textContent=fmtTime(data.last_checked_at);el('latest-version').textContent=latest?latest.version:'Unavailable';el('latest-published').textContent=latest?(latest.published_at||'—'):'—';el('release-notes').textContent=latest?(latest.body||'No release notes provided.'):'No release metadata available.';var badge=el('update-availability-badge');badge.textContent=data.update_available?'Update available':'Up to date';badge.className='vib-badge'+(data.update_available?' vib-badge-success':'');var pre=el('update-preflight-button');pre.disabled=!data.update_available||!!state.job;state.preflightVersion=null;el('update-install-button').disabled=true;el('preflight-summary').textContent='Not run';el('preflight-summary').className='vib-badge';el('preflight-results').innerHTML='<div class="licora-inline-info">Run Preflight to verify signature, PHP/extensions, storage, source permissions, disk space, and updater compatibility.</div>';if(data.warning)showPageError(data.warning);}
function renderPreflight(payload){var result=payload.preflight||{ok:false,checks:[]};var box=el('preflight-results');box.innerHTML='';(result.checks||[]).forEach(function(c){var row=document.createElement('div');row.className='licora-preflight-row';row.setAttribute('data-ok',c.ok?'1':'0');var icon=document.createElement('span');icon.className='licora-preflight-icon';icon.textContent=c.ok?'✓':'×';var label=document.createElement('span');label.className='licora-preflight-label';label.textContent=c.label;var detail=document.createElement('span');detail.className='licora-preflight-detail';detail.textContent=c.detail||'';row.append(icon,label,detail);box.appendChild(row);});var badge=el('preflight-summary');badge.textContent=result.ok?'Ready to install':'Blocked';badge.className='vib-badge '+(result.ok?'vib-badge-success':'vib-badge-danger');state.preflightVersion=result.ok?payload.version:null;el('update-install-button').disabled=!result.ok||!!state.job;}
function renderJob(job){state.job=job||null;var panel=el('active-job-panel'),open=el('update-open-logs');if(!job){panel.hidden=true;open.hidden=true;return;}panel.hidden=false;open.hidden=false;el('active-job-id').textContent=job.job_uuid;el('active-job-progress').style.width=Math.max(0,Math.min(100,job.progress||0))+'%';el('active-job-stage').textContent=(job.status||'')+' · '+(job.stage||'');el('active-job-percent').textContent=(job.progress||0)+'%';el('update-log-title').textContent='Licora Update — '+job.from_version+' → '+job.target_version;el('update-log-description').textContent=(job.status||'')+' · '+(job.stage||'');el('update-log-stage').textContent='Stage: '+(job.stage||'unknown')+' · '+(job.progress||0)+'%';el('update-download-diagnostics').href='ajax/update-diagnostics.php?job='+encodeURIComponent(job.job_uuid);var terminal=['success','failed','rolled_back'].indexOf(job.status)>=0;if(terminal){state.driving=false;el('update-preflight-button').disabled=!(state.update&&state.update.update_available);el('update-install-button').disabled=true;if(job.status==='success'){el('current-version').textContent=job.target_version;}if(job.error_message)showPageError(job.error_code+': '+job.error_message);}else{el('update-preflight-button').disabled=true;el('update-install-button').disabled=true;}}
function openModal(){el('licora-update-log-modal').hidden=false;document.body.style.overflow='hidden';if(el('update-log-pin').checked)scrollLogs();}
function closeModal(){el('licora-update-log-modal').hidden=true;document.body.style.overflow='';}
function scrollLogs(){var v=el('update-log-viewport');v.scrollTop=v.scrollHeight;}
function applyLogFilter(){var q=(el('update-log-search').value||'').toLowerCase();var level=el('update-log-filter').value;var rows=[].slice.call(el('update-log-viewport').querySelectorAll('.vib-log-row'));var visible=0;rows.forEach(function(row){var ok=(!level||row.dataset.level===level)&&(!q||row.textContent.toLowerCase().indexOf(q)>=0);row.hidden=!ok;if(ok)visible++;});el('update-log-count').textContent=visible+' / '+rows.length+' lines';}
function appendEvents(events){var viewport=el('update-log-viewport');var empty=el('update-log-empty');if(events.length&&empty)empty.remove();events.forEach(function(event){var row=document.createElement('div');row.className='vib-log-row';row.dataset.level=event.level||'info';row.dataset.eventId=event.id;var badge=document.createElement('span');badge.className='vib-log-badge';badge.textContent=event.level||'info';var time=document.createElement('span');time.className='vib-log-time';time.textContent='#'+event.id+' '+(event.created_at||'');var msg=document.createElement('span');msg.className='vib-log-message';msg.textContent='['+(event.stage||'update')+'] '+event.message;row.append(badge,time,msg);viewport.appendChild(row);state.totalEvents++;state.lastEventId=Math.max(state.lastEventId,Number(event.id)||0);});applyLogFilter();if(events.length&&el('update-log-pin').checked)scrollLogs();}
async function fetchEvents(){if(!state.job)return;var data=await api('ajax/update-events.php?job='+encodeURIComponent(state.job.job_uuid)+'&after='+state.lastEventId);appendEvents(data.events||[]);state.lastEventId=Math.max(state.lastEventId,data.last_id||0);}
function resetLogs(){state.lastEventId=0;state.totalEvents=0;var v=el('update-log-viewport');v.innerHTML='<div class="vib-log-empty" id="update-log-empty">Waiting for updater events…</div>';el('update-log-count').textContent='0 / 0 lines';}
async function driveJob(){if(state.driving||!state.job)return;if(['running','rollback_running'].indexOf(state.job.status)<0)return;state.driving=true;state.retryMs=700;while(state.driving&&state.job&&['running','rollback_running'].indexOf(state.job.status)>=0){try{await fetchEvents();var response=await api('ajax/update-step.php',{method:'POST',body:{job_uuid:state.job.job_uuid}});renderJob(response.job);await fetchEvents();state.retryMs=700;await sleep(500);}catch(err){showPageError((err.code?err.code+': ':'')+err.message);state.retryMs=Math.min(10000,state.retryMs*1.7);await sleep(state.retryMs);try{var status=await api('ajax/update-status.php?job='+encodeURIComponent(state.job.job_uuid));renderJob(status.job);}catch(ignore){}}}state.driving=false;try{await fetchEvents();await refreshHistory();}catch(ignore){} }
async function refreshHistory(){var data=await api('ajax/update-status.php');if(data.update)updateReleaseUI(data.update);if(data.active_job){renderJob(data.active_job);}else if(state.job&&['success','failed','rolled_back'].indexOf(state.job.status)>=0){/* preserve terminal job panel until reload */}renderHistory(data.history||[]);}
function statusClass(s){return s==='success'?'vib-badge-success':(s==='rolled_back'?'vib-badge-warning':(s==='failed'?'vib-badge-danger':''));}
function renderHistory(rows){var body=el('update-history-body');body.innerHTML='';if(!rows.length){body.innerHTML='<tr><td colspan="5">No updater jobs yet.</td></tr>';return;}rows.forEach(function(r){var tr=document.createElement('tr');tr.dataset.historyJob=r.job_uuid;var v=document.createElement('td');v.textContent=r.from_version+' → '+r.target_version;var st=document.createElement('td');var b=document.createElement('span');b.className='vib-badge '+statusClass(r.status);b.textContent=r.status;st.appendChild(b);var started=document.createElement('td');started.textContent=r.created_at||'—';var finished=document.createElement('td');finished.textContent=r.finished_at||'—';var actions=document.createElement('td');var d=document.createElement('a');d.className='vib-button';d.href='ajax/update-diagnostics.php?job='+encodeURIComponent(r.job_uuid);d.textContent='Diagnostics';actions.appendChild(d);if(r.status==='success'&&el('current-version').textContent===r.target_version){var rb=document.createElement('button');rb.type='button';rb.className='vib-button vib-button-danger';rb.style.marginLeft='5px';rb.dataset.updateRollback=r.job_uuid;rb.textContent='Rollback';actions.appendChild(rb);}tr.append(v,st,started,finished,actions);body.appendChild(tr);});}
async function checkUpdates(){showPageError('');el('update-check-button').disabled=true;try{var res=await api('ajax/update-check.php',{method:'POST',body:{force:true}});updateReleaseUI(res.data);if(res.active_job)renderJob(res.active_job);}catch(err){showPageError((err.code?err.code+': ':'')+err.message);}finally{el('update-check-button').disabled=false;}}
async function runPreflight(){var version=state.update&&state.update.latest&&state.update.latest.version;if(!version)return;showPageError('');var btn=el('update-preflight-button');btn.disabled=true;el('preflight-summary').textContent='Running…';try{var res=await api('ajax/update-preflight.php',{method:'POST',body:{target_version:version}});renderPreflight(res.data);}catch(err){showPageError((err.code?err.code+': ':'')+err.message);el('preflight-summary').textContent='Blocked';el('preflight-summary').className='vib-badge vib-badge-danger';el('update-install-button').disabled=true;}finally{if(!state.job)btn.disabled=!(state.update&&state.update.update_available);}}
async function installUpdate(){var version=state.update&&state.update.latest&&state.update.latest.version;if(!version||state.preflightVersion!==version)return;if(!confirm('Install Licora '+version+' now? A signed manifest will be re-verified and a rollback backup will be created before the critical update phase.'))return;showPageError('');el('update-install-button').disabled=true;try{var res=await api('ajax/update-start.php',{method:'POST',body:{target_version:version}});resetLogs();renderJob(res.job);openModal();driveJob();}catch(err){showPageError((err.code?err.code+': ':'')+err.message);}}
async function requestRollback(uuid){if(!confirm('Rollback this completed Licora update using its retained verified backup?'))return;showPageError('');try{var res=await api('ajax/update-rollback.php',{method:'POST',body:{job_uuid:uuid}});resetLogs();renderJob(res.job);openModal();driveJob();}catch(err){showPageError((err.code?err.code+': ':'')+err.message);}}
function copyLogs(){var rows=[].slice.call(el('update-log-viewport').querySelectorAll('.vib-log-row'));var text=rows.map(function(r){return r.textContent.trim();}).join('\n');if(!text)return;navigator.clipboard.writeText(text).then(function(){var b=el('update-copy-logs'),old=b.textContent;b.textContent='Copied';setTimeout(function(){b.textContent=old;},1200);}).catch(function(){showPageError('Copy failed. Select the log text manually.');});}
document.addEventListener('click',function(e){var rb=e.target.closest('[data-update-rollback]');if(rb){requestRollback(rb.dataset.updateRollback);}});
el('update-check-button').addEventListener('click',checkUpdates);el('update-preflight-button').addEventListener('click',runPreflight);el('update-install-button').addEventListener('click',installUpdate);el('update-open-logs').addEventListener('click',openModal);el('update-copy-logs').addEventListener('click',copyLogs);el('update-log-search').addEventListener('input',applyLogFilter);el('update-log-filter').addEventListener('change',applyLogFilter);el('update-log-pin').addEventListener('change',function(){if(this.checked)scrollLogs();});el('licora-update-log-modal').querySelector('[data-log-close]').addEventListener('click',closeModal);document.addEventListener('keydown',function(e){if(e.key==='Escape'&&!el('licora-update-log-modal').hidden)closeModal();});
if(state.job){resetLogs();renderJob(state.job);openModal();fetchEvents().then(driveJob).catch(function(){driveJob();});}else{updateReleaseUI(state.update||{});} 
})();
