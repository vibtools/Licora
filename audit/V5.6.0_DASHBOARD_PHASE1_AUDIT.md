# Licora v5.6.0 Dashboard Phase 1 Audit

## Frozen source authority

- Official baseline: Licora v5.5.1
- Baseline commit: `2f48ef569e6c532ab0de974a418c644e4ea8423f`
- Baseline tree: `c9986ba8b22f3d32c3b3d746dc24c7754d6d0132`
- Target: v5.6.0
- Scope: Dashboard Data Truth, Backend Read Model & Error Contract only.

## Implemented scope

1. Central read-only Dashboard data model.
2. Truthful license, device, API and expiration metrics.
3. API v1 and Secure API v2 activity kept source-identifiable.
4. Legacy `/api/check_license.php` non-inclusion made explicit rather than silently counted.
5. Measured health/config facts replace hardcoded operational claims.
6. Authenticated GET-only Dashboard JSON endpoint with sanitized structured errors.
7. Dedicated static/data contract test and disposable-DB integration test.
8. CI and tagged release MySQL gates include Dashboard integration validation.

## Frozen boundaries

No database migration, file deletion, license/device enforcement change, external API contract change, authentication/role change, installer change, Cron mutation change, updater protocol/state-machine change or shared UI architecture replacement is included.

## Phase boundary

The baseline `window.location.reload()` every 30 seconds remains intentionally present. Reload-free polling, manual refresh, last-updated/stale-state UX and compact Dashboard restructuring belong to Phase 2.

## Verification status

Populate after final validation:

- PHP syntax: PASS
- Dashboard contract: PASS
- Existing regression suite: PASS
- Dashboard DB integration: SKIPPED LOCALLY — dedicated MySQL environment unavailable; wired as mandatory CI/tagged-release gate
- Existing API v2/Admin v2/Updater DB integration: SKIPPED LOCALLY for the same environment reason
- Updater archive recovery: SKIPPED LOCALLY — PHP ZipArchive unavailable
- Git whitespace: PASS (`git diff --check`)
- Phase 1 decision: IMPLEMENTED, NOT YET COMPLETE — mandatory DB/CI gate pending
