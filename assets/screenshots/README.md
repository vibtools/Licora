# Screenshot Placeholders

Before adding screenshots:

- Use a disposable local database.
- Replace license keys, API keys, email addresses, usernames, IP addresses, device hashes, domains, and timestamps with fake values.
- Remove browser bookmarks and operating-system notifications.
- Use PNG format and descriptive names: `dashboard.png`, `licenses.png`, `api-keys.png`.
- Review image metadata before committing.
